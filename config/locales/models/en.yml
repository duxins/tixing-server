en:
  activerecord:
    errors:
      models:
        weibo/follower:
          attributes:
            base:
              taken: You have been following that user
        netease/monitoring:
          attributes:
            base:
              too_many: The maximum keyword limit has been reached (%{limit})
        jingdong/monitoring:
          attributes:
            base:
              too_many: The maximum product limit has been reached (%{limit})

